# Sessions - 2026-07

## 2026-07-03 Lwarp 网页笔记与 Workers 发布

- 新增 `main-web.tex`、`tex/preamble_web.tex` 和 `tex/styles/math1_web.tex`，为笔记库提供独立网页输出入口。
- 新增 `web/math1-web.css` 和 `scripts/build_web.ps1`，使用 `lwarp` 生成静态 HTML，并将输出整理到 `build/site/`。
- 新增 `wrangler.jsonc` 和 `package.json` 脚本，准备通过 Cloudflare Workers Static Assets 发布公开网页笔记。
- 保留 `main.tex` 与 PDF 样式链路不变，网页样式只重定义同名语义环境。
- 验证：`validate_math1_repo.py` 通过，确认原 PDF 构建链路仍可编译。
- 验证：`scripts/build_web.ps1` 成功生成 `index.html`、52 个 `note-*.html` 页面和合并后的 `math1-web.css`。
- 验证：`wrangler deploy --dry-run` 通过；本地 `wrangler dev --local --port 8787` 返回首页和章节页 200。
- 已发布：`https://kaoyan-math1-notes.sorenliu.workers.dev`，线上首页和 `note-1.html` 均返回 200，并命中 MathJax、CSS 和章节内容。

## 2026-07-06 Cloudflare Workers 静态资源刷新发布

- 按既有 `wrangler.jsonc` 配置继续使用 Cloudflare Workers Static Assets，不新建 Pages 项目或自定义域名。
- 验证：`npm run build:web` 成功生成 `build/site/`，共 53 个 HTML 页面。
- 验证：`wrangler deploy --dry-run` 通过，读取 55 个静态资源。
- 已发布：`npm run deploy:web` 成功上传并部署 `kaoyan-math1-notes`，版本 ID 为 `61436098-a453-4a98-abca-6cd45173f23a`。
- 线上验证：`https://kaoyan-math1-notes.sorenliu.workers.dev` 返回 HTTP 200，`/note-1.html` 跟随 307 到 `/note-1` 后返回 HTTP 200。

## 2026-07-15 LaTeX 响应式学习阅读器重设计

- 新增 `data/web_pages.yml`，固定 52 个内容页、讲次、源 TeX 与旧 `note-N` 映射；TeX 标题改用 `\studySection` / `\studySubsection`，PDF 中文标题保持不变。
- 新增构建后处理器、9 个原生阅读器模块、PWA 资源、黑白图标、搜索/内容清单、104 条重定向规则、Cloudflare headers 和静态/浏览器测试。
- 修复 Web TeX ligature 导致的 MathJax 智能引号问题；MathJax 3.2.2 与所需扩展已完全自托管，行内、块级、aligned 与 matrix 运行时样例通过三浏览器渲染。
- 视觉外壳按批准稿校准：桌面贴边三栏、平板双栏、手机显式进度与目录抽屉；讲次目录使用学科/讲次层级及 1.1–1.12 编号，元信息卡与页内锚点按阅读顺序呈现。
- 完整构建通过：53 个 HTML（首页 + 52 内容页）；最终 `buildId` 为 `bab13cfe341ec055d497`；原子预缓存 83 项、4,979,112 bytes，低于 10 MiB。
- PDF 验证通过：A4、58 页、435,352 bytes，最终构建 SHA-256 为 `0d08f793b717766b04b6038ce0fb65cc8a1b2a2cbe6966c753304fad0aeb1880`；抽查首页、中间页与末页，无裁切、重叠或缺字。
- 静态门禁 8/8 通过；Playwright 48 项中 17 项执行通过、31 项按设备条件跳过，覆盖 Edge/Chrome/Firefox 与 360/390/768/1280/1440 宽度、公式、搜索、抽屉、44px 触控区、200% 缩放等效视口、reduced-motion、偏好、打印、axe、完整 53 页离线遍历和旧网址离线导航。
- Service Worker 旧网址映射不再克隆 `navigate` Request；版本确认后，所有已受控标签页会在新 Worker 接管时统一刷新，避免旧 DOM 与新缓存混用，首次安装仍保持当前页面。
- 应用内浏览器以 1440×960、390×844 和手机目录抽屉逐项对照批准稿；确认黑白色值、三栏/抽屉、标题、元信息、进度、目录编号与公式呈现。
- `wrangler deploy --dry-run` 通过并读取 103 个静态文件；经用户明确授权后已部署到 `https://kaoyan-math1-notes.sorenliu.workers.dev`，Cloudflare 版本 ID 为 `0ba3d18f-7279-4a0a-9f80-c8e7169b78db`，未推送 Git。
- 线上验证通过：首页、canonical 页面、内容清单、Service Worker、自托管 MathJax 和 PDF 均返回 200，旧 `/note-1.html` 返回 301 到新 slug；线上 `buildId` 为 `bab13cfe341ec055d497`，PDF 哈希与本地构建一致。
- 线上 Playwright 验收通过：1440px 桌面 6 项通过、2 项按设备条件跳过；390px 手机 3 项通过、5 项按设备条件跳过；覆盖公式、搜索、目录抽屉、布局、打印、axe 与完整离线遍历，无失败项。

## 2026-07-15 Agent 与数学 Skill 现代化

- 将根 `AGENTS.md` 缩减为稳定项目契约，把数学任务路由、成功标准、停止条件和条件化检查集中到 `kaoyan-math1-fullscore-coach`。
- 回答深度与文件持久化正式分离：“简洁回答”仍维护题库，“只聊天不写文件”才跳过写入；聊天结构改为按题目复杂度自适应。
- 以 `data/textbook_catalog.yml` 和 `tex/templates/` 为唯一事实源，新增知识点/错题实时模板，删除 Skill 内重复资产、便携 bootstrap、百分制自评规则和独立总提示词。
- validator、查重与下一题号脚本改用共享 YAML/逐题锚点解析；validator 新增 `--root`、`--no-compile`、`--format text|json`、稳定 0/1/2 退出码，并区分 `PASS` 与 `SKIP`。
- 新增 13 项 `unittest` 契约测试和 8 类行为案例，覆盖无效 YAML、重复 ID、缺章、错误路径、缺失锚点、悬空引用、依赖错误、编译器缺失、简单/综合题、错解、OCR、重复题、简洁/只聊天模式及非数学元任务。
- 验证：Skill Creator `quick_validate.py` 通过；契约测试 13/13 通过；静态 validator 12 PASS / 1 SKIP，完整 validator 13/13 PASS，并由 `latexmk` 成功编译 PDF。
- 验证：`npm run build:web` 成功生成 53 个 HTML 页面与同步 PDF；`npm run test:static` 8/8 通过。因未修改阅读器运行时，按计划未运行浏览器回归。
- 本次未修改数学题解、题号、registry 数据或网页视觉；未推送、未部署。

## 2026-07-15 `pee.esoren.com/math` 自定义域名发布

- 新建独立同级项目 `C:\Users\liuso\Documents\pee-gateway`：TypeScript、Node 24、Wrangler JSONC、独立 npm 锁文件与 Workers Vitest 4 测试；Custom Domain 首页提供数学和 `https://english.esoren.com/` 两个入口。
- 网关实现 `GET/HEAD /`、保留查询参数的 `/math` 308、404/405、安全响应头、Observability 与 `workers_dev: false`；最终版本 ID 为 `6973b15d-ee69-4ec3-a417-fbc2810e875f`。
- 数学网页统一迁移到 `/math`：构建产物进入 `build/site/math/`，发布元文件保留在 `build/site/`；canonical、导航、搜索、PDF、MathJax、页面上下文、manifest、Service Worker、预缓存和旧编号映射均使用 `/math/...`。
- HTML 新增 `no-transform`，阻止 Cloudflare 自动注入 `static.cloudflareinsights.com` beacon；真实 Edge、Chrome 与 Firefox 再验均无外部请求，网关 DOM 保持 0 个脚本。
- 完整 Web 构建通过：53 个 canonical HTML 页面；最终 `buildId` 为 `6cbba9e4ecca343905e9`；PWA 预缓存 84 项、4,999,319 bytes。
- 数学静态测试 9/9 通过；本地 Playwright 矩阵 17 项通过、31 项按设备条件跳过；Wrangler dry-run 读取 105 个发布文件并通过。
- 网关 `wrangler types`、TypeScript、Workers 运行时测试 6/6 与 Wrangler dry-run 均通过；依赖审计报告 0 个漏洞。
- 按“网关 Custom Domain → 数学暂存 Route → 最终关闭 workers.dev”顺序发布；数学 Worker 最终版本 ID 为 `4386736b-12fa-4b58-b48c-b791e8b8f47e`。
- 生产 Playwright 最终矩阵 17 项通过、31 项按设备条件跳过；在线验证网关、`/math` 查询参数跳转、数学首页、代表章节、搜索、PDF、MathJax、PWA、离线完整语料与旧编号 301 均通过。
- `https://kaoyan-math1-notes.sorenliu.workers.dev/` 及其 `/math/` 均返回 404；未修改数学正文、catalog、registry 或 PDF 内容，未推送远端。
