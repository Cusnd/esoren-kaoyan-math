# Requirements

## 2026-06-28

- 初始化一个用于考研数学一长期学习的 LaTeX 题库与笔记仓库。
- 根目录放置 `AGENTS.md`，约束后续 Codex 按“讲解 + 写入 TeX + 更新索引”的方式工作。
- 按高等数学、线性代数、概率论与数理统计拆分章节文件，保证每章单独一个 `.tex` 文件。
- 提供题目模板、题目登记 YAML、题目/方法/错题/公式索引。
- 默认使用 XeLaTeX 编译中文文档。
- 根据用户提供的教材目录图片，将章节结构调整为高等数学 18 讲 + 附录、线性代数 0-6 讲、概率论与数理统计 1-6 讲。
- 保存教材目录映射，后续新增内容优先按教材讲次归档。
- 默认回答进入“详细教学 + 严谨整理 + 可复盘沉淀”模式，数学题必须包含任务判断、定位、直觉入口、严谨解答、合法性检查、考场写法、变式迁移、易错点、TeX 整理、文件更新和复盘任务。
- 在仓库内实现 `kaoyan-math1-fullscore-coach` Codex Skill，封装考研数学一满分讲解、TeX 题库整理、索引维护、错因复盘和验证脚本。

## 2026-07-15

- 按已批准的极简黑白视觉稿重设计编译后 LaTeX 网页阅读器：Windows 桌面三栏、平板双栏、800px 以下正文直达与目录抽屉。
- 保留 `TeX → lwarp → 静态 HTML`，不引入 SPA、账号系统、数据库或后端；PDF 作为同步构建的补充下载。
- 固定 52 个内容页的 canonical slug、中文标题、学科、讲次、旧 `note-N` 编号与源 TeX 映射；保留 104 条旧网址兼容规则。
- 修复 lwarp MathJax 配置中的智能引号污染，固定并自托管 MathJax 3.2.2，断网时不得产生跨域公式请求。
- 提供构建期 HTML 后处理、分组首页、全文搜索、页内大纲、前后篇、阅读偏好、阅读进度、打印视图、PDF 与 PWA 离线阅读。
- 偏好和进度只保存在当前浏览器；固定存储键 `math1.reader.preferences.v1` 与 `math1.reader.progress.v1`，存储失败不得阻断阅读。
- 生成共享 `schemaVersion` / `buildId` 的内容清单、搜索索引与页面上下文；全量原子预缓存必须低于 10 MiB，PDF 不进入强制预缓存。
- 补齐键盘、焦点管理、触控尺寸、200% 缩放、reduced-motion、安全区、打印和 WCAG AA 验收。
- 固定 Node.js 24 与构建依赖版本；Windows PowerShell 是正式构建入口。未经另行授权不得部署或推送。

## 2026-07-15 Agent 与 Skill 现代化

- 根 `AGENTS.md` 只保留稳定项目契约；数学任务的细节工作流集中到 `kaoyan-math1-fullscore-coach`。
- 回答结构按任务复杂度自适应，但严谨性、条件检查、可迁移方法和考场得分点仍是硬性成功标准。
- 回答深度与文件持久化是两个独立开关：“简洁回答”仍维护文件，只有“只聊天不写文件”才禁止写入。
- `data/textbook_catalog.yml` 和 `tex/templates/` 分别作为章节路由与实时模板的唯一事实源；不再维护 Skill 内重复资产或便携初始化脚本。
- 新题必须同步章节正文、题目索引与 `problem_registry.yml`；知识点不得创建虚假题号，重复题必须补充原题而非分配新 ID。
- validator 必须确定性检查 37 个章节、PDF/Web 入口、目录文档、registry、逐题锚点、题目索引与悬空引用，并提供文本/JSON 输出和稳定退出码。
- 使用标准库 `unittest` 固化仓库契约，提交简单题、综合题、错解、OCR、重复题、简洁模式、只聊天模式与非数学元任务的代表性行为案例。
- 删除独立总提示词 `prompts/codex_math1_prompt.md`；不修改全局 Codex 指令，不新增生产依赖。

## 2026-07-18 GPT-5.6 学习系统

- 提示层保持一个根 `AGENTS.md` 和一个仓库数学 Skill；按需引用学习协议、归档分流和仓库编辑规则，不复制平行总提示词。
- 使用一个题目 registry 与共享题号空间，以 `core | practice` 区分主库和练习库；现有题号、章节位置和旧网页地址全部保持不变。
- 主库收录母题、新方法、关键知识和真实错题；普通变式与训练进入独立练习库。通用陷阱使用稳定 pitfall 节点，不冒充用户错题。
- catalog 为 37 个讲次提供稳定 `chapter_key`；新增知识 registry，以稳定节点和显式边连接知识、方法、陷阱与题目。
- 分别生成主笔记、练习册、答案册 PDF；统一 Web 展示两库，主 PDF 不得混入练习题，草稿不得进入公开构建。
- 阅读器保持静态多页架构，增加两库检索、一跳知识关系与浏览器本地复习提醒；不增加账号、后端、网页 AI 或答题评分。
- 复习状态使用固定本地日历阶梯并支持版本化导入导出；阅读滚动不得自动推断掌握程度。
- 提示词评测包含 24 个可执行案例，模型与推理档位参数化；运行开始时冻结一次仓库白名单快照，所有写入只发生在它的一次性副本中，并可在同一基础树上只切换提示层。
