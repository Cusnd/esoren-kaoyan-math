# Requirements

## 2026-06-28

- 初始化一个用于考研数学一长期学习的 LaTeX 题库与笔记仓库。
- 根目录放置 `AGENTS.md`，约束后续 Codex 按“讲解 + 写入 TeX + 更新索引”的方式工作。
- 按高等数学、线性代数、概率论与数理统计拆分章节文件，保证每章单独一个 `.tex` 文件。
- 提供题目模板、题目登记 YAML、题目/方法/错题/公式索引。
- 默认使用 XeLaTeX 编译中文文档。
- 根据用户提供的教材目录图片，将章节结构调整为高等数学 18 讲 + 附录、线性代数 0-6 讲、概率论与数理统计 1-6 讲。
- 保存教材目录映射，后续新增内容优先按教材讲次归档。
- 默认回答进入“详细教学 + 严谨整理 + 可复盘沉淀”模式，数学题必须包含任务判断、定位、直觉入口、严谨解答、合法性检查、考场写法、变式迁移、易错点、TeX 整理、文件更新和复盘任务。
- 在仓库内实现 `kaoyan-math1-fullscore-coach` Codex Skill，封装考研数学一满分讲解、TeX 题库整理、索引维护、错因复盘和验证脚本。

## 2026-07-15

- 按已批准的极简黑白视觉稿重设计编译后 LaTeX 网页阅读器：Windows 桌面三栏、平板双栏、800px 以下正文直达与目录抽屉。
- 保留 `TeX → lwarp → 静态 HTML`，不引入 SPA、账号系统、数据库或后端；PDF 作为同步构建的补充下载。
- 固定 52 个内容页的 canonical slug、中文标题、学科、讲次、旧 `note-N` 编号与源 TeX 映射；保留 104 条旧网址兼容规则。
- 修复 lwarp MathJax 配置中的智能引号污染，固定并自托管 MathJax 3.2.2，断网时不得产生跨域公式请求。
- 提供构建期 HTML 后处理、分组首页、全文搜索、页内大纲、前后篇、阅读偏好、阅读进度、打印视图、PDF 与 PWA 离线阅读。
- 偏好和进度只保存在当前浏览器；固定存储键 `math1.reader.preferences.v1` 与 `math1.reader.progress.v1`，存储失败不得阻断阅读。
- 生成共享 `schemaVersion` / `buildId` 的内容清单、搜索索引与页面上下文；全量原子预缓存必须低于 10 MiB，PDF 不进入强制预缓存。
- 补齐键盘、焦点管理、触控尺寸、200% 缩放、reduced-motion、安全区、打印和 WCAG AA 验收。
- 固定 Node.js 24 与构建依赖版本；Windows PowerShell 是正式构建入口。未经另行授权不得部署或推送。
