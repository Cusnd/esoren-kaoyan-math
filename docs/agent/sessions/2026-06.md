# Sessions - 2026-06

## 2026-06-28

- 根据用户提供的初始化说明创建考研数学一 LaTeX 笔记与题解库骨架。
- 新增根目录 `AGENTS.md`、`README.md`、`main.tex`、`.gitignore`。
- 新增高等数学、线性代数、概率论与数理统计的章节 `.tex` 文件。
- 新增题目、方法、错题、公式索引和题目模板。
- 新增 `data/problem_registry.yml` 与 `prompts/codex_math1_prompt.md`。
- 验证：使用 TeX Live 2026 的 `latexmk -xelatex` 编译 `main.tex` 成功，输出 `build/main.pdf`。
- 验证：`git diff --cached --check` 通过，staged diff 只包含初始化源文件。

## 2026-06-28 教材目录同步

- 根据用户提供的教材目录图片，将 `main.tex` 和章节文件调整为教材讲次结构。
- 高等数学调整为第 1-18 讲，并新增附录 1-6 的 TeX 文件。
- 线性代数新增第 0 讲，并按第 1-6 讲维护。
- 概率论与数理统计按第 1-6 讲维护。
- 新增 `docs/textbook_catalog.md` 与 `data/textbook_catalog.yml`，记录讲次、标题、起始页和对应 TeX 文件。
- 验证：`main.tex` 的所有 `\input{...}` 文件均存在，旧通用章节文件名无残留引用。
- 验证：使用 TeX Live 2026 的 `latexmk -xelatex` 编译 `main.tex` 成功，输出 `build/main.pdf`。

## 2026-06-28 题目整理：MATH1-CALC-0001

- 整理用户图片中的教材例 1.1：由 \(f(u+1/u)\) 反求 \(f(x)\)。
- 写入高等数学第 1 讲，并同步更新题目索引、方法索引和题目登记表。
- 记录题面符号校注：截图疑似有负号，但参考答案对应分子为 \(u+u^3\) 的版本。
- 验证：`git diff --check` 通过；使用 TeX Live 2026 的 `latexmk -xelatex` 编译 `main.tex` 成功。

## 2026-06-28 规则更新：详细输出增强

- 根据用户提供的“通用详细输出增强模板”，更新 `AGENTS.md` 和 `prompts/codex_math1_prompt.md`。
- 将默认输出升级为“详细教学 + 严谨整理 + 可复盘沉淀”模式。
- 明确数学题、知识点、错解三类任务的输出结构，以及合法性检查、考场满分写法、变式迁移、易错点、文件更新和复盘建议要求。
- 验证：`git diff --check` 通过；本次仅修改 Markdown 指令与项目日志，未重新编译 LaTeX。

## 2026-06-28 Skill 生成：kaoyan-math1-fullscore-coach

- 根据 ChatGPT Pro 输出，在 `.agents/skills/kaoyan-math1-fullscore-coach` 创建仓库级 Codex Skill。
- 新增 `SKILL.md`、`agents/openai.yaml`、5 个 references、6 个 assets 和 4 个 scripts。
- Skill 目标：触发考研数学一满分讲解、TeX 归档、题目/方法/错题/公式索引维护、`problem_registry.yml` 更新和验证检查。
- `references/chapter-routing.md` 已适配当前教材讲次目录，而不是旧通用章节路径。
- 根 `AGENTS.md` 顶部新增显式调用提示 `$kaoyan-math1-fullscore-coach`。
- 验证：Skill Creator `quick_validate.py` 在 `codex-tools` 环境中通过。
- 验证：4 个 Skill 脚本通过 Python 编译检查。
- 验证：`bootstrap_math1_repo.py` 幂等运行，当前仓库文件均为已存在，未覆盖内容。
- 验证：`validate_math1_repo.py` 通过；在 `codex-tools` 环境中成功解析 `problem_registry.yml` 并完成 LaTeX 编译。
- 验证：`next_problem_id.py --subject calc` 输出 `MATH1-CALC-0002`；`find_duplicate_problem.py` 可检测到样例与 `MATH1-CALC-0001` 可能重复。

## 2026-06-28 讲解与索引补全：MATH1-CALC-0001

- 用户要求讲解教材例 1.1：由 \(f(u+1/u)\) 反求 \(f(x)\)。
- 检查发现该题已写入高等数学第 1 讲、题目索引、方法索引和题目登记表，未重复录入。
- 补全公式索引，新增倒数型整体换元恒等式 \(u^2+\frac{1}{u^2}=(u+\frac{1}{u})^2-2\) 及其使用条件、易错点和关联题目。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过，成功解析 `problem_registry.yml` 并完成 LaTeX 编译。

## 2026-06-28 聊天公式渲染规则修复

- 根据用户截图反馈，当时临时收紧 Codex 桌面端聊天公式写法，优先保证数学内容可读。
- 在 `AGENTS.md`、长期 prompt、专项 Skill 和 Skill 质量门禁中新增聊天公式渲染约定。
- 该临时约束已在 2026-06-29 根据完整 LaTeX 渲染测试更新为标准 LaTeX 写法。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过，成功解析 `problem_registry.yml` 并完成 LaTeX 编译。

## 2026-06-28 补充解法：MATH1-CALC-0001

- 根据用户追问“有另一种做法吗”，为 MATH1-CALC-0001 追加“反解参数后消元”补充解法。
- 在章节题解中说明设 \(t=u+\frac{1}{u}\) 后得到 \(u^2-tu+1=0\)，再利用 \(1+u^2=tu\) 与 \(1+u^4=u^2(t^2-2)\) 消去参数。
- 同步更新方法索引，新增“反解参数后消元”方法及其适用场景、分支检查提醒。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过，成功解析 `problem_registry.yml` 并完成 LaTeX 编译。

## 2026-06-28 分支反解追问：MATH1-CALC-0001

- 根据用户追问“能否直接解一元二次方程得到 \(u=\theta(t)\) 再代回”，补充说明显式解根代回的可行性与分支风险。
- 在章节题解中指出 \(u=\frac{t\pm\sqrt{t^2-4}}{2}\) 有两个分支，不能无检查地当作单值函数；本题因两个分支互为倒数且右端表达式取值相同，所以可以代回。
- 同步更新方法索引中的“反解参数后消元”使用提醒。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过，成功解析 `problem_registry.yml` 并完成 LaTeX 编译。

## 2026-06-28 题目整理：MATH1-CALC-0002

- 整理用户图片中的教材例 1.2：由含 \(f(x)\) 与 \(f(1/x)\) 的函数关系式求 \(f(x)\)。
- 写入高等数学第 1 讲，题型为“对偶代换建立方程组”，核心步骤是令 \(x\mapsto \frac{1}{x}\) 得到第二个方程并消去 \(f(1/x)\)。
- 同步更新题目索引、方法索引、公式索引和 `data/problem_registry.yml`。
- 公式索引新增倒数代换中的根式化简提醒：\(x>0\) 时 \(\sqrt{1+\frac{1}{x^2}}=\frac{\sqrt{1+x^2}}{x}\)，一般情形不能忽略绝对值。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过，成功解析 `problem_registry.yml` 并完成 LaTeX 编译。

## 2026-06-29 知识点整理：反函数

- 根据用户提供的教材片段，整理高等数学第 1 讲“反函数的定义、存在条件与图像关系”。
- 重点补充反函数定义、定义域与值域互换、一一对应条件、严格单调的充分非必要性、图像关于 \(y=x\) 对称以及水平线判别法。
- 使用 \(y=x^2, x\ge 0\) 说明限制定义域后才有反函数；使用分段函数 \(f(x)=x\ (x\ge 0), f(x)=1/x\ (x<0)\) 说明有反函数不一定单调。
- 同步更新方法索引，新增“求反函数五步法”和“水平线判别法”。
- 同步更新公式索引，新增反函数复合恒等式及变量范围提醒。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过，成功解析 `problem_registry.yml` 并完成 LaTeX 编译。

## 2026-06-29 LaTeX 渲染约束更新

- 根据用户完成的 LaTeX 渲染测试，确认 Codex 桌面端已正常显示行内公式、块级公式、分数、指数、上下标、希腊字母与数学符号、积分、极限、求和、连乘、矩阵与行列式、分段函数、多行对齐公式、概率统计公式、向量微积分公式、集合符号、优化约束表达式、张量指标记号、Dirac bra-ket 记号和复杂嵌套表达式。
- 更新 `AGENTS.md`、长期 prompt、专项 Skill、LaTeX 风格说明和质量门禁，将旧的“避免行内公式和特定定界符”约束改为“允许标准 LaTeX，按可读性选择行内或块级公式”。
- 本次只更新说明与约束，不修改数学题解正文、章节 TeX、索引或 `problem_registry.yml`。
- 验证：旧限制文本检查无残留；新标准 LaTeX 规则已覆盖主说明、长期 prompt、专项 Skill 和质量门禁；`git diff --check` 通过；`validate_math1_repo.py` 在 `codex-tools` 环境中通过并完成 LaTeX 编译。

## 2026-06-29 LaTeX 模板拆分与视觉升级

- 根据用户要求，将 PDF 视觉样式从 `tex/preamble.tex` 拆分到 `tex/styles/academic_old_money.tex`，让内容 TeX 保持数学语义友好。
- 将 `tex/preamble.tex` 改为轻量入口：加载中文、数学基础依赖，再输入独立样式模板。
- 新视觉方向为克制学术、现代高级、少量老钱风：深墨绿、暖灰、古金棕和低饱和酒红仅用于标题与强调，替代旧的亮蓝/亮绿/亮红笔记容器。
- 同步更新 Skill 样式资产、bootstrap 初始化脚本、LaTeX 风格说明、README、AGENTS 结构说明和长期决策记录。
- 根据用户反馈进一步取消彩色框包裹正文，将四类语义环境改为无边框、无底色的普通可分页段落块，只保留轻量标题标签。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过并完成 LaTeX 编译；结构检查确认章节与索引 TeX 未新增样式定义，旧彩色笔记配色无残留；抽样查看标题页、目录页、长知识点页和题目/题解/易错点页，视觉符合克制学术方向。

## 2026-06-29 题目元信息弱化

- 根据用户截图反馈，题目编号、来源、题型、知识点、难度等管理字段存在感过强，压过真正题面。
- 新增 `problemMeta` 轻量元信息环境，仅用于收纳题目前置管理字段；保留 `problemBox` 无边框正文段落风格。
- 同步更新现有两道题、题目模板、Skill 模板、`AGENTS.md` 和 LaTeX 风格说明；题面校注等影响解题条件的信息仍保留在题面正文附近。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过并完成 LaTeX 编译；结构检查确认未重新引入 `tcolorbox`/大彩色框方案，章节与索引 TeX 未新增样式定义；抽样查看题目页，元信息已降为紧凑浅框，题面成为视觉主体。

## 2026-06-29 屏幕阅读字号与间距调整

- 根据用户反馈，当前 PDF 在屏幕上阅读偏密，将 `main.tex` 文档基准字号从 `11pt` 提升到 `12pt`。
- 放松正文行距、段间距、展示公式上下间距、列表间距、章节标题间距、语义块间距和 `problemMeta` 内边距。
- 同步更新 Skill 的 `main-template.tex` 与 `academic-old-money-style.tex`，保持后续初始化模板一致。
- 验证：`validate_math1_repo.py` 在 `codex-tools` 环境中通过并完成 LaTeX 编译；PDF 从 21 页增至 24 页；结构检查确认未重新引入大彩色框方案，章节与索引 TeX 未新增样式定义；抽样查看目录页、正文页和题目页，屏幕可读性改善。
